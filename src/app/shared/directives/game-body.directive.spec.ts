import { GameBodyDirective } from './game-body.directive';

describe('GameBodyDirective', () => {
  it('should create an instance', () => {
    const directive = new GameBodyDirective();
    expect(directive).toBeTruthy();
  });
});
